[project]
name = "crotal"
version = "0.1.6"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]",
    "python-jose",
    "itsdangerous",
    "pydantic-settings",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"